# Ralph Progress Log — Agentic Issue-Triage Agent
# branch: ralph/agentic-issue-triage
# Each entry: a completed user story (US-###), what shipped, and the verification that gated it.
# Format mirrors Ralph's autonomous-run log: one fresh implementation pass per story, TDD red->green->commit.

--- run start ---
US-001 DONE — scaffold package + label allow-list (ALLOWED set + validate_labels) — 3 tests green
US-002 DONE — verdict schema validation (validate_verdict + VALID_TYPES/SEVERITY/ROUTES) — 3 tests green
US-003 DONE — read-only filesystem tools (read_file/list_files/grep + READONLY_REGISTRY/SCHEMAS) — 4 tests green
US-004 DONE — run_command tool for fix-it-man (timeout-bounded) + FIXIT_REGISTRY/SCHEMAS — 7 tests green
US-005 DONE — GitHub Models HTTP client (call_model + 429 exponential backoff) — 3 tests green
US-006 DONE — hand-rolled agent loop (run_agent + parse_tool_calls + dispatch) — 3 tests green
US-007 DONE — gh-CLI helpers for GitHub write ops (post_comment/add_labels/has_label) — 3 tests green
US-008 DONE — model-agnostic persona prompts (triage + fix-it-man + stoic-developer) — 3 tests green
US-009 DONE — Stage 1 entrypoint (run_triage.py): idempotency guard, verdict validation, label filtering, failure backstop; DEVIATION: load_persona uses _PERSONA_DIR = Path(__file__).resolve().parent / "personas" (module-relative, not CWD-relative) so tests using monkeypatch.chdir(tmp_path) don't FileNotFoundError — 3 tests green
US-010 DONE — Stage 2 advisory worker (run_worker.py): route→persona dispatch via PERSONA_BY_ROUTE, failure backstop, same module-relative load_persona deviation; minor test lambda fix (setdefault→update in test spy to avoid truthy short-circuit) — 2 tests green; full suite 30/30 green
US-011 DONE — idempotent label setup script (bash -n clean) — not executed (live op)
